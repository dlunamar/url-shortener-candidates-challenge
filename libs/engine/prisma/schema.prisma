generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model ShortUrl {
  code        String   @id
  originalUrl String
  clicks      Int      @default(0)
  createdAt   DateTime @default(now())

  @@map("short_urls")
}
